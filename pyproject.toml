[project]
name = "solar-env"
version = "0.1.0"
description = "Solar Energy Optimization RL Environment using OpenEnv"
authors = [
    {name = "Chaitanya"}
]
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "openai"
]

[tool.openenv]
env = "env:SolarEnv"
